 deletes the first element from an array. a program that should prompt the user to enter 
 the elements and then display the updated array after deleting the first element.


 
#include <iostream>
using namespace std;
int main(){
    int n;
    cin >> n;
    int arr[20];
    for(int i=0; i<n; i++){
        cin >> arr[i];
    }
    for(int i=1; i<n; i++){
        cout<<arr[i];
        if(i != n-1) cout<<" ";
        
    }
    return 0;
    
}
